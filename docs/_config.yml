# Site settings
title: Live for fun
SEOTitle: Live for fun
header-img: img/home-bg.jpg
email: shen_shuaijia@163.com
description: ''
keyword: ''
url: 'https://blog.shenshuaijia.com' # your host, for absolute URL
baseurl: '' # for example, '/blog' if your blog hosted on 'host/blog'

# SNS settings
RSS: false
github_username: hokkaidosunny

# Build settings
# from 2016, 'pygments' is unsupported on GitHub Pages. Use 'rouge' for highlighting instead.
highlighter: rouge
permalink: pretty
paginate: 10
exclude: ['less', 'node_modules', 'Gruntfile.js', 'package.json', 'README.md']
anchorjs: true # if you want to customize anchor. check out line:181 of `post.html`

# Gems
# from PR#40, to support local preview for Jekyll 3.0
plugins: [jekyll-paginate]

# Markdown settings
# replace redcarpet to kramdown,
# although redcarpet can auto highlight code, the lack of header-id make the catalog impossible, so I switch to kramdown
# document: http://jekyllrb.com/docs/configuration/#kramdown
markdown: kramdown
kramdown:
  input: GFM # use Github Flavored Markdown !important

# Disqus settings
disqus_username: shenshuaijia
layout: default
comments: true

# Google Analytics
ga_track_id: 'UA-129869121-1' # Format: UA-xxxxxx-xx

# Sidebar settings
sidebar: true # whether or not using Sidebar.
sidebar_about_description: '做人呢，最重要的就是开心。'
sidebar-avatar: /img/avatar-ssj.jpg # use absolute URL, seeing it's used in both `/` and `/about/`

# Featured Tags
featured-tags: true # whether or not using Feature-Tags
featured-condition-size: 1 # A tag will be featured if the size of it is more than this condition value

theme: jekyll-theme-minimal